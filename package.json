{
  "name": "shopreturn",
  "version": "1.0.0",
  "private": true,
  "description": "ShopReturn - returns management for a Dhaka e-commerce seller, built on SELISE Blocks. The web app lives in app/; this root manifest exists because the Blocks build pipeline installs dependencies from the repository root.",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "build": "cd app && npm ci && npm run build",
    "lint": "cd app && npm run lint",
    "test": "cd app && npm test",
    "dev": "cd app && npm run dev"
  }
}
